import { PageHeader, Field, SelectField, SubmitButton } from "@/components/app-shell/ui";
import { requireClinicSection } from "@/lib/auth/session";
import { getFinanceSettings, listFinanceRows, money, monthPeriod } from "@/lib/finance/service";
import { ExportCsvLink, FinancePage, FinanceTable, SchemaNotice } from "../shared";
import { transferAction } from "../actions";
export default async function MovimentosPage({searchParams}) { const {activeClinic}=await requireClinicSection("financeiro"); const p=monthPeriod((await searchParams)?.month); const [r,s]=await Promise.all([listFinanceRows("finance_movimentos",activeClinic.id,{start:p.start,end:p.end,dateColumn:"data_movimento",limit:300}),getFinanceSettings(activeClinic.id)]); return <FinancePage><PageHeader eyebrow="Financeiro" title="Movimentações e extrato" description="Entradas e saídas efetivas. Transferências são neutras no resultado."/><div className="mt-4"><ExportCsvLink report="movimentacoes" month={p.month}/></div>{!r.available?<div className="mt-6"><SchemaNotice/></div>:<><div className="mt-6"><FinanceTable rows={r.rows} columns={[{key:"data_movimento",label:"Data",render:x=>new Date(x.data_movimento).toLocaleDateString("pt-BR")},{key:"descricao",label:"Descrição"},{key:"tipo",label:"Tipo"},{key:"valor_liquido",label:"Valor",render:x=><strong className={x.tipo.includes("entrada")?"text-emerald-700":"text-red-700"}>{money(x.valor_liquido)}</strong>},{key:"provider",label:"Origem"}]}/></div><form action={transferAction} className="premium-panel mt-6 rounded-lg p-5"><h2 className="text-lg font-black">Transferir entre contas</h2><div className="mt-4 grid gap-4 md:grid-cols-4"><SelectField label="Origem" name="conta_origem_id" required><option value="">Selecione</option>{s.accounts.map(a=><option key={a.id} value={a.id}>{a.nome}</option>)}</SelectField><SelectField label="Destino" name="conta_destino_id" required><option value="">Selecione</option>{s.accounts.map(a=><option key={a.id} value={a.id}>{a.nome}</option>)}</SelectField><Field label="Valor" name="valor" type="number" required/><Field label="Descrição" name="descricao"/><div className="md:col-span-4"><SubmitButton>Transferir</SubmitButton></div></div></form></>}</FinancePage>; }
